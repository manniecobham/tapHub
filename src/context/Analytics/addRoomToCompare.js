// Get API call for room to be compared

const jsonRequest = {
  userId: 1,
  userToken: "12345",
  roomId: 2,
};

const jsonResponseSuccess = {
  success: true,
  message: "Room can be compared",
};

const jsonResponseFailure = {
  success: true,
  message: "Room can't be compared",
};
