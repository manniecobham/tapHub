// Post API call for deleting user
// not a priority

const jsonRequest = {
  userId: 1,
  userToken: "12345",
  // password for confirmation - if wrong password, account is not deleted
  password: "password",
};

const jsonResponse = {
  success: true,
};
