// Post API call for deleting user
// not a priority

const jsonRequest = {
  username: "testuser",
  userToken: "12345",
  // password for confirmation - if wrong password, account is not deleted
  password: "password",
};

const jsonResponse = {
  success: true,
};
